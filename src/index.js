import React from 'react';
import ReactDOM from 'react-dom';
import BirthdayVouchers from './App';

ReactDOM.render(
  <React.StrictMode>
    <BirthdayVouchers />
  </React.StrictMode>,
  document.getElementById('root')
);
